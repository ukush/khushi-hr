{
  "name": "khushi-hr",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "verify": "bash ./verify-setup.sh",
    "lint": "eslint --ext .ts,.tsx frontend backend || true"
  },
  "engines": {
    "node": ">=18"
  }
}
